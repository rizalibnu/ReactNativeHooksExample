import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputWrapper: {
    flexDirection: "row",
    alignItems: "center",
    paddingBottom: 20
  },
  input: {
    borderColor: "#ddd",
    borderWidth: 1,
    marginLeft: 10
  },
  image: {
    width: 150,
    height: 150,
    marginVertical: 10
  }
});

export default styles;
